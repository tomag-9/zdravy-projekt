# Core Django
Django==5.1.15
djangorestframework==3.15.2
django-cors-headers==4.3.1
# Pinned to 5.5.1: fixes CVE – missing user validation in for_user() allowed
# disabled accounts to retain API access via previously issued JWTs.
# Versions < 5.5.1 do not re-check is_active on token use.
# Do not downgrade below 5.5.1 without reviewing the simplejwt 5.4–5.5 release notes.
djangorestframework-simplejwt==5.5.1

# Database
psycopg2-binary==2.9.9

# Celery / async tasks
celery==5.4.0
redis==5.2.1
django-redis==5.4.0
django-celery-beat==2.7.0

# Testing
pytest==7.4.3
pytest-django==4.7.0
pytest-cov==4.1.0
factory-boy==3.3.0

# Development tools
django-extensions==3.2.3

# XLSX / PDF export
openpyxl==3.1.5
reportlab==4.2.5

# Web Push Notifications (VAPID)
pywebpush==2.0.0

# Production server
gunicorn==23.0.0
whitenoise==6.9.0

# requests compatibility: avoid RequestsDependencyWarning with chardet>=6
chardet==5.2.0

# Environment variables
python-decouple==3.8

# API Documentation
drf-spectacular==0.28.0

# Observability
django-prometheus==2.3.1
sentry-sdk==2.27.0

# Code quality
black==26.3.1
flake8==7.0.0
isort==5.13.2
mypy==1.11.2
